// Define and export a function called Joke.
// It should accept a joke object as a parameter and return an html string

export const jokeSetup = (taco) => {
    return `
    <p>${taco.setup}</p>
    `
}

